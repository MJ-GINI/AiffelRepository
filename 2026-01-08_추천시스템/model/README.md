1. `autoInt_model_weights.h5`: AutoInt 기본 가중치 파일
2. `autoInt_model_weights.weights.h5`: AutoInt+ 가중치 파일 (파일 로드는 잘 되는데, Mac 환경에서 계속 오류가 나서 스트림릿 로딩 실패)
3. `autoInt_model_weights.pkl`: 2번과 동일한 AutoInt+ 가중치 .h5 파일을 .pkl(순수 숫자 값만 추출해서 이식하는 방법(Numpy Transfer))으로 진행
